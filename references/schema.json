{
  "version": "3.1",
  "components": {
    "schemas": {
      "NewPlanInput": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Plan display name; omitted = \"Route plan <date> <time>\"."
          }
        },
        "required": [],
        "description": "Input for `routing24_new_plan`: commit a fresh EMPTY plan, replacing the\nloaded one (the app asks the user to confirm when the loaded plan holds\ndata, and unsaved changes are auto-saved first). Follow with the\n`routing24_upsert_*` tools to create the depot/vehicles/stops, then solve\nwith `routing24_reoptimize_plan`."
      },
      "NewPlanResult": {
        "type": "object",
        "properties": {
          "created": {
            "const": true
          },
          "planUuid": {
            "type": "string"
          }
        },
        "required": [
          "created",
          "planUuid"
        ],
        "description": "Result of `routing24_new_plan`: the fresh empty plan is now the loaded one."
      },
      "SolveStarted": {
        "type": "object",
        "properties": {
          "started": {
            "const": true
          },
          "planUuid": {
            "type": "string"
          },
          "solveRun": {
            "type": "number",
            "description": "1-based counter of solve launches in this session. Each launch is a NEW\nrun even when the request repeats — two identical re-optimizes are two\ndistinct solves, and this field is what makes their results distinct."
          },
          "paidFeatures": {
            "$ref": "#/components/schemas/PaidFeatureReport",
            "description": "Present only when the plan uses paid features outside this account."
          },
          "warnings": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Non-blocking notes about how the request was interpreted (e.g. an\nall-zero cost model replaced by the default rates of 1 per mile/km\nplus 1 per hour). Omitted when there is nothing to say."
          },
          "timeLimitS": {
            "type": "number",
            "description": "The solver's time budget for this run, in seconds (`options.time_limit_s`\nor the size-based default). Geocoding and matrix time come on top, so the\nrun lands shortly after this many seconds of solving."
          }
        },
        "required": [
          "started",
          "planUuid",
          "solveRun"
        ],
        "description": "Result of `routing24_reoptimize_plan`: the solve is running, poll for progress."
      },
      "PaidFeatureReport": {
        "type": "object",
        "properties": {
          "locked": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Paid capabilities the plan uses that this account's plan does not\ninclude, as `Feature` member names (e.g. `\"Skills\"`, `\"DriverBreaks\"`)."
          },
          "stripped": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The subset DROPPED from this solve because the free allowance for paid\nfeatures is spent. Empty while the allowance lasts. A non-empty list\nmeans the routes ignore those constraints — say so when reporting the\nplan, and name the upgrade as the way to get them back."
          },
          "freeRunsLeft": {
            "type": "number",
            "description": "Free solves left that may use locked features (0 once spent)."
          }
        },
        "required": [
          "locked",
          "stripped",
          "freeRunsLeft"
        ],
        "description": "What the solve did with the paid features the plan uses. Absent when the\nplan uses none, or when every one of them is included in the user's\nsubscription — the common case, and nothing to report."
      },
      "OptimizeStatus": {
        "type": "object",
        "properties": {
          "phase": {
            "$ref": "#/components/schemas/OptimizePhase"
          },
          "running": {
            "type": "boolean"
          },
          "progress": {
            "type": "number",
            "description": "0..1 while solving, when available."
          },
          "feasible": {
            "type": "boolean"
          },
          "routes": {
            "type": "number"
          },
          "stops": {
            "type": "number"
          },
          "unassignedCount": {
            "type": "number"
          },
          "unassignedSummary": {
            "type": "string",
            "description": "One-line WHY for a nonzero `unassignedCount` (from the last computed\ndiagnostics), so every status poll keeps the reasons one hop away. The\nper-stop reasons, numeric targets and fixes come from\n`routing24_unassigned`."
          },
          "distance": {
            "type": "number"
          },
          "distanceUnit": {
            "oneOf": [
              {
                "const": "km"
              },
              {
                "const": "mi"
              }
            ]
          },
          "durationHours": {
            "type": "number"
          },
          "problemsCount": {
            "type": "number",
            "description": "Total constraint-problem markers across all routes (0 = feasible)."
          },
          "driftFromOptimized": {
            "$ref": "#/components/schemas/OptimizedDrift",
            "description": "Drift vs the last full optimization (absent while a solve runs)."
          },
          "error": {
            "type": "string"
          },
          "planUuid": {
            "type": "string"
          },
          "complete": {
            "type": "boolean",
            "description": "True when the solve ran to completion (not cancelled part-way)."
          },
          "cost": {
            "$ref": "#/components/schemas/SolutionCost",
            "description": "Economic cost (money) of the whole plan. Coverage is `unassignedCount`,\nnever a cost. Absent on pre-feature solutions."
          },
          "objective": {
            "$ref": "#/components/schemas/SolverObjective",
            "description": "Solver comparison scalar — see {@link SolverObjective}; never money."
          },
          "costModel": {
            "$ref": "#/components/schemas/CostModel",
            "description": "The per-unit pricing this solve used, fallbacks included — see\n{@link CostModel}. Absent mid-solve and on pre-feature solutions."
          },
          "routeStats": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RouteBrief"
            },
            "description": "One compact entry per route (empty routes included), in on-screen order:\nvehicle, stop count, distance, duration, feasibility and cost per route —\nNO stop list. Drill into one route's ordered stops with `routing24_route`."
          },
          "revision": {
            "type": "number",
            "description": "Session revision the routes reflect (present once an editing session is\nopen); bumps on every committed edit."
          },
          "undoDepth": {
            "type": "number",
            "description": "Committed edits `routing24_undo` can walk back."
          },
          "redoDepth": {
            "type": "number",
            "description": "Undone edits `routing24_redo` can re-apply."
          },
          "dataSync": {
            "$ref": "#/components/schemas/DataSyncStatus",
            "description": "Present ONLY when the plan data (or the distance unit) changed after\nthe last optimization — see {@link DataSyncStatus}."
          }
        },
        "required": [
          "phase",
          "running"
        ]
      },
      "OptimizePhase": {
        "oneOf": [
          {
            "const": "idle"
          },
          {
            "const": "geocoding"
          },
          {
            "const": "matrix"
          },
          {
            "const": "solving"
          },
          {
            "const": "saving"
          },
          {
            "const": "done"
          },
          {
            "const": "error"
          }
        ]
      },
      "OptimizedDrift": {
        "type": "object",
        "properties": {
          "optimizedAt": {
            "type": "number",
            "description": "When the baseline solve completed (ms since epoch)."
          },
          "distancePct": {
            "type": "number",
            "description": "Travel-distance change vs the baseline, percent (+ = longer)."
          },
          "durationPct": {
            "type": "number",
            "description": "Total-duration change vs the baseline, percent (+ = slower)."
          },
          "distance": {
            "type": "number",
            "description": "Distance change in `distanceUnit` (+ = longer)."
          },
          "durationHours": {
            "type": "number",
            "description": "Duration change in hours (+ = slower)."
          },
          "cost": {
            "type": "number",
            "description": "ECONOMIC cost change, money (+ = more expensive). Coverage drift is\n`unassigned` (a count) — the two are never merged into one number.\nAbsent when the baseline carried no cost."
          },
          "costPct": {
            "type": "number",
            "description": "Cost change vs the baseline, percent (+ = more expensive)."
          },
          "problems": {
            "type": "number",
            "description": "New problem markers vs the baseline (+ = worse)."
          },
          "unassigned": {
            "type": "number",
            "description": "Newly unassigned stops vs the baseline (+ = worse)."
          },
          "severity": {
            "oneOf": [
              {
                "const": "improved"
              },
              {
                "const": "neutral"
              },
              {
                "const": "degraded"
              },
              {
                "const": "severe"
              }
            ],
            "description": "Fixed-threshold verdict: any new problem/unassigned ⇒ at least\n`degraded`; cost, distance or duration > +5% ⇒ `degraded`, > +15% ⇒\n`severe`; everything ≤ baseline with a ≥1% gain ⇒ `improved`; else\n`neutral`."
          },
          "summary": {
            "type": "string",
            "description": "One ready-to-quote sentence, lexicographic: coverage first, then money."
          }
        },
        "required": [
          "optimizedAt",
          "distancePct",
          "durationPct",
          "distance",
          "durationHours",
          "problems",
          "unassigned",
          "severity",
          "summary"
        ],
        "description": "How far the CURRENT plan has drifted from the last full optimization —\ncovering every change since that solve, the user's manual edits included.\nRecompute-free: the baseline is frozen at solve completion; only a new\n`routing24_reoptimize_plan` (or the user's OPTIMIZE button) resets it. Absent when\nthe plan predates the baseline feature or was never solved.\n\nREPORTING OBLIGATION: when `severity` is `degraded` or `severe`, tell the\nuser (quote `summary`) and stop there. An edit the user asked for is\nexpected to cost more — never re-optimize (or propose it) to walk one back;\nre-optimizing is only ever the user's own request."
      },
      "SolutionCost": {
        "type": "object",
        "properties": {
          "total": {
            "type": "number",
            "description": "Full economic cost = Σ `components[].amount` (2-decimal rounding)."
          },
          "components": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CostComponent"
            },
            "description": "The solver's own decomposition of `total` — every charged line, zero\nlines omitted. Plan-level lines carry amounts and plan-wide quantities\nbut NO rates (rates can differ per vehicle — read\n`OptimizeStatus.costModel`, or one route's cost from `routing24_route`,\nfor rate-level verification)."
          }
        },
        "required": [
          "total",
          "components"
        ],
        "description": "ECONOMIC cost of the solution, in the task's money units (the unit costs\nconfigured on the vehicles). Report money to the user from here. It does\nNOT include unassigned-order penalties — coverage is a count\n(`unassignedCount`), not a cost. Absent on pre-feature solutions."
      },
      "CostComponent": {
        "type": "object",
        "properties": {
          "kind": {
            "oneOf": [
              {
                "const": "fixed"
              },
              {
                "const": "distance"
              },
              {
                "const": "duration"
              },
              {
                "const": "overtime"
              },
              {
                "const": "stop"
              },
              {
                "const": "rideOvertime"
              },
              {
                "const": "loadDistance"
              },
              {
                "const": "other"
              }
            ],
            "description": "`fixed` per-use vehicle cost; `distance` travel priced per mi/km;\n`duration` route time priced per hour, EXCLUDING the overtime premium —\n`overtime` (the surcharge for hours past the regular limit) is its own\nline, so lines always sum to `total`; `stop` per-visit fees;\n`rideOvertime` linked orders' ride time inside their overtime band;\n`loadDistance` carriage (load on board × leg length); `other`\nunattributed remainder (rare — solutions saved by older app versions)."
          },
          "amount": {
            "type": "number",
            "description": "Money — an addend of the parent `total`. Report this."
          },
          "rate": {
            "type": "number",
            "description": "Effective per-`unit` rate the solve used (on `routing24_route` costs\nonly) — the authored rate at the engine's fixed-point step (1e-6 per\nwire unit), rounded to 2 decimals here, so it normally equals the\nvehicle's configured value. See `defaultRate` before presenting it as\nconfigured."
          },
          "quantity": {
            "type": "number",
            "description": "Billed quantity, in `unit`."
          },
          "unit": {
            "oneOf": [
              {
                "const": "km"
              },
              {
                "const": "mi"
              },
              {
                "const": "stop"
              },
              {
                "const": "h"
              },
              {
                "const": "load-mi"
              },
              {
                "const": "load-km"
              }
            ],
            "description": "Unit of `quantity` (and denominator of `rate`). Distance units are the\nSOLVE-TIME display unit; `load-mi`/`load-km` = load unit × mile/km."
          },
          "defaultRate": {
            "const": true,
            "description": "The `rate` is an engine DEFAULT — the matching vehicle cost field is\nblank in the app, the engine supplied the value (see\n{@link VehicleEffectiveRates}.`defaultRates`). Call it a default;\nnever present it as a cost the user configured."
          }
        },
        "required": [
          "kind",
          "amount"
        ],
        "description": "One line of a cost total. Self-describing: a future pricing element arrives\nas a new `kind`, never a new shape. `amount` is the solver's authoritative\nmoney value; when `rate` and `quantity` are present, `amount` ≈ `rate` ×\n`quantity` (2-decimal rounding; a force-placed stop on a restricted leg may\nlegitimately price below that — `amount` wins)."
      },
      "SolverObjective": {
        "type": "object",
        "properties": {
          "total": {
            "type": "number",
            "description": "cost.total + unassignedPenalty. Comparisons only."
          },
          "unassignedPenalty": {
            "type": "number",
            "description": "Σ synthetic penalties of unassigned orders (incl. alternatives not chosen)."
          }
        },
        "required": [
          "total",
          "unassignedPenalty"
        ],
        "description": "The scalar the solver minimizes — SOLVER UNITS, NOT MONEY, never quote it\nto the user. Each unassigned order carries a synthetic penalty priced\nabove any possible serving cost, so fewer-unassigned always outranks\ncheaper-routes. Use it ONLY to compare two states of the same plan:\nlower `objective.total` = better plan. Equivalent hand rule: fewer\nunassigned wins; ties break on lower `cost.total`."
      },
      "CostModel": {
        "type": "object",
        "properties": {
          "priced": {
            "type": "boolean"
          },
          "note": {
            "type": "string",
            "description": "Ready-to-quote explanation, present when `priced` is false."
          },
          "distanceUnit": {
            "oneOf": [
              {
                "const": "km"
              },
              {
                "const": "mi"
              }
            ],
            "description": "Solve-time display distance unit the rates are stated in."
          },
          "vehicles": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/VehicleEffectiveRates"
            },
            "description": "Effective per-unit rates per vehicle type."
          }
        },
        "required": [
          "priced",
          "distanceUnit",
          "vehicles"
        ],
        "description": "The pricing this solve ACTUALLY used — the vehicles' authored cost rates\nafter the engine's fallbacks. `priced: false` means NO vehicle prices\ndistance, duration or load-distance: the optimizer then minimizes distance\nand time together (every vehicle priced at the defaults of 1 per mile/km\nplus 1 per hour), so `cost.total` reads roughly as the plan's travel\ndistance in miles/km plus its total route time in hours (driving + service\n+ waiting) — quote `note` when explaining. Absent on pre-feature solutions."
      },
      "VehicleEffectiveRates": {
        "type": "object",
        "properties": {
          "vehicleId": {
            "type": "string"
          },
          "fixed": {
            "type": "number"
          },
          "distance": {
            "type": "number"
          },
          "duration": {
            "type": "number"
          },
          "stop": {
            "type": "number"
          },
          "overtime": {
            "type": "number"
          },
          "rideOvertime": {
            "type": "number"
          },
          "loadDistance": {
            "type": "number"
          },
          "defaultRates": {
            "type": "array",
            "items": {
              "oneOf": [
                {
                  "const": "distance"
                },
                {
                  "const": "duration"
                },
                {
                  "const": "overtime"
                }
              ]
            },
            "description": "Rates the ENGINE supplied — the vehicle's matching cost field is blank\nin the app: `distance` and `duration` when the fleet is unpriced (the\ndefaults of 1 per mile/km and 1 per hour), `overtime` when overtime is\nallowed but unpriced (priced at the vehicle's hourly rate, else a\nnominal 1/hour). Call these defaults; never present one as a cost the\nuser configured."
          }
        },
        "required": [
          "vehicleId"
        ],
        "description": "One vehicle type's effective rates, in `CostModel.distanceUnit` and hours:\n`fixed` per route driven, `distance` per mi/km, `duration` / `overtime` /\n`rideOvertime` per hour, `stop` per stop served, `loadDistance` per load\nunit per mi/km. An omitted rate = 0 (not charged). Rates are what the\nengine BILLS — the authored value at the engine's fixed-point step (1e-6\nper wire unit), rounded to 2 decimals here, so a rate normally equals the\nauthored one; the authored values themselves are on the vehicle rows\n(`routing24_list_vehicles`)."
      },
      "RouteBrief": {
        "type": "object",
        "properties": {
          "cost": {
            "$ref": "#/components/schemas/RouteCost",
            "description": "Economic cost of this route (absent on pre-feature solutions)."
          },
          "distance": {
            "type": "number",
            "description": "Total travel distance of the route, in `distanceUnit`."
          },
          "route": {
            "type": "number",
            "description": "1-based route number, matching the on-screen order — the SAME number\nevery route-addressed tool takes (`routing24_route`,\n`routing24_reoptimize_route`, the `routing24_edit_*` `to_route`/`route`/\n`target`/`sources` fields). Numbers renumber after\n`routing24_edit_remove_routes`;\nre-read them from the returned state."
          },
          "vehicleId": {
            "type": "string",
            "description": "Id of the vehicle serving this route."
          },
          "stopCount": {
            "type": "number",
            "description": "Count of site stops served (the depot start/end are excluded)."
          },
          "durationHours": {
            "type": "number",
            "description": "Total route duration (travel + service + wait), hours."
          },
          "feasible": {
            "type": "boolean",
            "description": "True when every constraint on this route is satisfied."
          },
          "problemsCount": {
            "type": "number",
            "description": "Constraint-problem markers on this route (0 = feasible); read the\nproblems themselves from `routing24_route`."
          }
        },
        "required": [
          "distance",
          "route",
          "stopCount",
          "durationHours"
        ],
        "description": "One route's compact stats in {@link OptimizeStatus}.`routeStats` — totals\nonly, no stop list. Drill into the ordered stops with `routing24_route(route)`.\nThe shared fields are {@link PlanSolutionRoute}'s, defined once there."
      },
      "RouteCost": {
        "type": "object",
        "properties": {
          "total": {
            "type": "number"
          },
          "components": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CostComponent"
            }
          }
        },
        "required": [
          "total",
          "components"
        ],
        "description": "One route's ECONOMIC cost, in the task's money units. `total` =\nΣ `components[].amount`. In `routeStats` and session route summaries the\nlines carry amounts only; `routing24_route` adds the serving vehicle's\neffective `rate`/`quantity`/`unit` per line."
      },
      "DataSyncStatus": {
        "type": "object",
        "properties": {
          "stops": {
            "$ref": "#/components/schemas/EntityKindDiff"
          },
          "vehicles": {
            "$ref": "#/components/schemas/EntityKindDiff"
          },
          "depots": {
            "$ref": "#/components/schemas/EntityKindDiff"
          },
          "addresses": {
            "$ref": "#/components/schemas/EntityKindDiff"
          },
          "distanceUnitChanged": {
            "type": "boolean",
            "description": "The display distance unit changed since the solve (miles <-> km), which\nalone re-prices the solution's distances."
          }
        },
        "required": [],
        "description": "What changed in the plan data AFTER the last optimization — the same\ncondition that shows the user the NOT SYNC badge. The current solution and\nthe route-editing tools still see the plan as of that solve; the changes\ncounted here take effect on the next optimization (run it only when the\nuser asks). A kind that is absent is unchanged."
      },
      "EntityKindDiff": {
        "type": "object",
        "properties": {
          "added": {
            "type": "integer",
            "minimum": 0
          },
          "removed": {
            "type": "integer",
            "minimum": 0
          },
          "changed": {
            "type": "integer",
            "minimum": 0
          }
        },
        "required": [
          "added",
          "removed",
          "changed"
        ],
        "description": "Counts of one entity kind's rows that differ from the last optimization's\nsnapshot of the plan."
      },
      "RouteInput": {
        "type": "object",
        "properties": {
          "route": {
            "type": "integer",
            "minimum": 1,
            "description": "1-based route number from `OptimizeStatus.routeStats[].route` — the\nsame number shown to the user (Route 1 = `route: 1`)."
          }
        },
        "required": [
          "route"
        ],
        "description": "Input for `routing24_route`: one route's full detail by its 1-based `route`\nnumber."
      },
      "PlanSolutionRoute": {
        "type": "object",
        "properties": {
          "route": {
            "type": "number",
            "description": "1-based route number, matching the on-screen order — the SAME number\nevery route-addressed tool takes (`routing24_route`,\n`routing24_reoptimize_route`, the `routing24_edit_*` `to_route`/`route`/\n`target`/`sources` fields). Numbers renumber after\n`routing24_edit_remove_routes`;\nre-read them from the returned state."
          },
          "vehicleId": {
            "type": "string",
            "description": "Id of the vehicle serving this route."
          },
          "stopCount": {
            "type": "number",
            "description": "Count of site stops served (the depot start/end are excluded)."
          },
          "distance": {
            "type": "number",
            "description": "Total travel distance of the route, in `distanceUnit`."
          },
          "durationHours": {
            "type": "number",
            "description": "Total route duration (travel + service + wait), hours."
          },
          "startTimeS": {
            "type": "number",
            "description": "When the route starts (depot departure, or arrival at the first\norder on an open-start route), seconds since midnight."
          },
          "endTimeS": {
            "type": "number",
            "description": "When the route ends (depot return, or service completion at the last\norder on an open-ended route), seconds since midnight."
          },
          "feasible": {
            "type": "boolean",
            "description": "True when every constraint on this route is satisfied."
          },
          "problems": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PlanProblem"
            },
            "description": "Route-level constraint problems (also pinned per-stop)."
          },
          "cost": {
            "$ref": "#/components/schemas/RouteCost",
            "description": "Economic cost of this route (absent on pre-feature solutions)."
          },
          "stops": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PlanSolutionStop"
            },
            "description": "Stops in visit order; bounded by depot stops unless the vehicle's\nstart/finish depot is empty (open-ended)."
          }
        },
        "required": [
          "route",
          "stopCount",
          "distance",
          "durationHours",
          "stops"
        ],
        "description": "One route: an ordered stop sequence, bounded by depot stops when the\nvehicle has a start/finish depot (open-ended ends start/finish at an order\ninstead). Empty routes (a vehicle with no stops, e.g. just created by\n`routing24_edit_create_route`) are included with `stopCount: 0` so `route` stays a\ncomplete, editable address space."
      },
      "PlanProblem": {
        "type": "object",
        "properties": {
          "type": {
            "$ref": "#/components/schemas/PlanProblemType"
          },
          "amount": {
            "type": "number",
            "description": "How far over the constraint (see {@link PlanProblemType} for units)."
          },
          "dimension": {
            "type": "number",
            "description": "Capacity dimension index (`capacity` problems only)."
          },
          "classes": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Conflicting load-class names (`incompatible_load_class` only)."
          }
        },
        "required": [
          "type"
        ],
        "description": "One constraint problem (common.fbs `Problem`)."
      },
      "PlanProblemType": {
        "oneOf": [
          {
            "const": "capacity"
          },
          {
            "const": "max_distance"
          },
          {
            "const": "vehicle_incompatible"
          },
          {
            "const": "unreachable"
          },
          {
            "const": "time_window"
          },
          {
            "const": "max_duration"
          },
          {
            "const": "depot_time_window"
          },
          {
            "const": "shift_window"
          },
          {
            "const": "precedence"
          },
          {
            "const": "driving_allowance"
          },
          {
            "const": "break_schedule"
          },
          {
            "const": "break_location"
          },
          {
            "const": "sequence"
          },
          {
            "const": "incompatible_load_class"
          },
          {
            "const": "shelf_life"
          },
          {
            "const": "field_not_applicable"
          }
        ],
        "description": "Constraint kinds a problem can report (common.fbs `ConstraintType`).\n`amount` units: seconds for the time kinds, load units for `capacity`,\ndistance for `max_distance`."
      },
      "PlanSolutionStop": {
        "type": "object",
        "properties": {
          "seq": {
            "type": "number",
            "description": "0-based position within the route (0 = the starting depot)."
          },
          "type": {
            "oneOf": [
              {
                "const": "depot"
              },
              {
                "const": "site"
              },
              {
                "const": "break"
              }
            ],
            "description": "The depot, a delivery/pickup site, or a scheduled driver break. A\n`break` stop is a solver-planned pause taken AT the previous stop's\nlocation: it has no `id`/`address`, `serviceDurationS` is the pause\nlength, and its travel-leg fields are 0."
          },
          "id": {
            "type": "string",
            "description": "The site/depot id (the id you passed to optimize, or an auto-assigned one)."
          },
          "address": {
            "type": "string",
            "description": "Matched street address, when known."
          },
          "arrivalTimeS": {
            "type": "number",
            "description": "Arrival time, seconds since midnight (physical driver timeline)."
          },
          "departureTimeS": {
            "type": "number",
            "description": "End of service (arrival + wait + service), seconds since midnight."
          },
          "serviceDurationS": {
            "type": "number",
            "description": "Time spent servicing this stop, seconds."
          },
          "waitDurationS": {
            "type": "number",
            "description": "Wait before the stop's time window opens, seconds (0/absent = none)."
          },
          "legDistance": {
            "type": "number",
            "description": "Distance of the travel leg arriving at this stop, in `distanceUnit`."
          },
          "legDurationS": {
            "type": "number",
            "description": "Duration of the travel leg arriving at this stop, seconds."
          },
          "carriedLoad": {
            "type": "number",
            "description": "Vehicle load carried on arrival."
          },
          "deliveredLoad": {
            "type": "number",
            "description": "Load delivered at this stop."
          },
          "pickedUpLoad": {
            "type": "number",
            "description": "Load picked up at this stop."
          },
          "problems": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PlanProblem"
            },
            "description": "Constraint problems materializing at this stop (absent = none)."
          },
          "marginalCost": {
            "type": "number",
            "description": "Removal saving — how much cheaper (money) / shorter (seconds) this\nroute gets without this stop, same vehicle kept. An estimate for the\nCURRENT stop order: savings are NOT additive across stops and change\nafter any edit or re-optimize. Absent on depots and pre-feature plans."
          },
          "marginalDurationS": {
            "type": "number"
          }
        },
        "required": [
          "seq",
          "type",
          "arrivalTimeS",
          "departureTimeS",
          "serviceDurationS",
          "legDistance",
          "legDurationS",
          "carriedLoad",
          "deliveredLoad",
          "pickedUpLoad"
        ]
      },
      "UnassignedInput": {
        "type": "object",
        "properties": {
          "refresh_diagnostics": {
            "type": "boolean",
            "description": "Recompute the unassigned-site diagnostics before returning them (runs an\nengine pass; only needed when `diagnosticsStale` was true)."
          }
        },
        "required": [],
        "description": "Input for `routing24_unassigned`."
      },
      "UnassignedReport": {
        "type": "object",
        "properties": {
          "available": {
            "type": "boolean",
            "description": "`false` when the loaded plan carries no optimized solution yet."
          },
          "unassignedCount": {
            "type": "number",
            "description": "Number of site stops that could not be served."
          },
          "unassigned": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Ids of the unassigned sites, if any."
          },
          "alternativesNotChosen": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Group-alternative sites not chosen (a sibling serves the group) — these\nare NOT failures."
          },
          "userUnassigned": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Unassigned site ids that were removed by the user/agent (not the solver)."
          },
          "diagnosticsStale": {
            "type": "boolean",
            "description": "True when `unassignedDiagnostics` predates the latest edits — pass\n`refresh_diagnostics: true` to recompute before reading it."
          },
          "unassignedDiagnostics": {
            "$ref": "#/components/schemas/UnassignedDiagnosticsReport",
            "description": "Why sites are unassigned, in prose (capped; see `truncated`/`omitted`)."
          },
          "insertionQuotes": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/InsertionQuote"
            },
            "description": "Ready-to-serve quotes for the unassigned orders (the cheapest feasible\nplacement for each quotable site; none when nothing fits). Refreshed with\nthe diagnostics — pass `refresh_diagnostics: true` when `diagnosticsStale`."
          }
        },
        "required": [
          "available"
        ],
        "description": "Result of `routing24_unassigned`: which sites of the loaded plan are unserved\nand why. `available` is `false` when the plan has no solution yet."
      },
      "UnassignedDiagnosticsReport": {
        "type": "object",
        "properties": {
          "summary": {
            "type": "string",
            "description": "One-paragraph overview of the unassigned situation."
          },
          "truncated": {
            "type": "boolean",
            "description": "True when the per-site list was capped (see `omitted`)."
          },
          "sites": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "site": {
                  "type": "string"
                },
                "category": {
                  "type": "string",
                  "description": "Coarse cause (e.g. hard incompatibility vs fleet contention)."
                },
                "explanation": {
                  "type": "string"
                },
                "blockers": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "Site ids standing in the way (competition for the same capacity)."
                },
                "levers": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "Constraint changes that would let this site be served."
                }
              },
              "required": [
                "site",
                "category",
                "explanation"
              ]
            }
          },
          "omitted": {
            "type": "object",
            "properties": {
              "sitesCount": {
                "type": "number"
              },
              "dominantKind": {
                "type": "string"
              },
              "dominantConstraint": {
                "type": "string"
              }
            },
            "required": [
              "sitesCount"
            ],
            "description": "Rollup of the sites the cap left out."
          }
        },
        "required": [
          "summary",
          "sites"
        ],
        "description": "Prose explanation of why sites are unassigned (common.fbs\n`UnassignedDiagnosticsLlm` — the agent-facing projection, capped in size)."
      },
      "InsertionQuote": {
        "type": "object",
        "properties": {
          "stop": {
            "type": "string"
          },
          "route": {
            "type": "number",
            "description": "1-based route number of the quoted insertion point."
          },
          "vehicleId": {
            "type": "string"
          },
          "cost": {
            "type": "number",
            "description": "Route-cost increase, money."
          },
          "durationS": {
            "type": "number",
            "description": "Route-duration increase, seconds."
          },
          "before": {
            "type": "string"
          },
          "after": {
            "type": "string"
          },
          "deliveryAfter": {
            "type": "string",
            "description": "Pair quote (stop = the pickup): where the DELIVERY step is quoted, after\nthe pickup (precedence-aware). Absent for a single order."
          }
        },
        "required": [
          "stop",
          "cost",
          "durationS"
        ],
        "description": "The cheapest FEASIBLE way to serve one unassigned order in the current\nroutes — REAL units (money / seconds), safe to quote to the user. Directly\nreplayable with `routing24_edit_move_stops`: use `before`/`after` as the anchor (or\n`to_route: route` with `placement: \"best\"` when both are absent).\n`route` absent = a spare `vehicleId` vehicle serves it on a fresh\nsingle-visit route, its fixed cost included (an existing route's fixed\ncost is sunk and excluded). Estimates for the CURRENT arrangement only:\nquotes are NOT additive across orders and go stale with any edit or\nre-optimize (`diagnosticsStale`)."
      },
      "EditMoveStopsInput": {
        "type": "object",
        "properties": {
          "stops": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "description": "Stop business ids to move, kept together as one block."
          },
          "before": {
            "type": "string",
            "description": "Anchor stop id: insert the block immediately before it."
          },
          "after": {
            "type": "string",
            "description": "Anchor stop id: insert the block immediately after it."
          },
          "to_route": {
            "type": "integer",
            "minimum": 1,
            "description": "Destination route number, 1-based (see `OptimizeStatus.routeStats[].route`)."
          },
          "placement": {
            "oneOf": [
              {
                "const": "append"
              },
              {
                "const": "best"
              }
            ],
            "description": "With `to_route`: `append` to the end, or `best` = cheapest insertion."
          },
          "user_assigned": {
            "type": "boolean",
            "description": "Mark the stops user-assigned (badge + problem report). Default true."
          }
        },
        "required": [
          "stops"
        ],
        "description": "Input for `routing24_edit_move_stops`: move/re-plan planned OR unassigned\nstops. Anchor with `before`/`after` (a planned stop id on the destination\nroute), or target a route with `to_route` + `placement`."
      },
      "EditUnassignStopsInput": {
        "type": "object",
        "properties": {
          "stops": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "description": "Stop business ids to remove from their routes."
          },
          "user_unassigned": {
            "type": "boolean",
            "description": "Stamp the \"removed by you\" marker (default true). Pass false to unassign\nwithout claiming the removal, keeping the stop's original reason."
          }
        },
        "required": [
          "stops"
        ],
        "description": "Input for `routing24_edit_unassign_stops`."
      },
      "EditSetRouteVehicleInput": {
        "type": "object",
        "properties": {
          "route": {
            "type": "integer",
            "minimum": 1,
            "description": "Route number, 1-based (see `OptimizeStatus.routeStats[].route`)."
          },
          "vehicle": {
            "type": "string",
            "description": "Vehicle id; rejects `vehicle_overused` when its count is exhausted."
          }
        },
        "required": [
          "route",
          "vehicle"
        ],
        "description": "Input for `routing24_edit_set_route_vehicle`."
      },
      "EditCreateRouteInput": {
        "type": "object",
        "properties": {
          "vehicle": {
            "type": "string",
            "description": "Vehicle id for the new empty route (appended last)."
          }
        },
        "required": [
          "vehicle"
        ],
        "description": "Input for `routing24_edit_create_route`."
      },
      "EditRemoveRoutesInput": {
        "type": "object",
        "properties": {
          "routes": {
            "type": "array",
            "items": {
              "type": "integer",
              "minimum": 1
            },
            "minItems": 1,
            "description": "Route numbers (1-based) to delete. The remaining routes renumber —\nre-read them after."
          }
        },
        "required": [
          "routes"
        ],
        "description": "Input for `routing24_edit_remove_routes`. The stops on those routes are\nunassigned first, in the SAME atomic batch — the engine's own remove op\naccepts empty routes only."
      },
      "EditSplitRouteInput": {
        "type": "object",
        "properties": {
          "route": {
            "type": "integer",
            "minimum": 1,
            "description": "Route number (1-based) to split."
          },
          "after": {
            "type": "string",
            "description": "Stop id that becomes the last stop of the head route."
          },
          "vehicle": {
            "type": "string",
            "description": "Vehicle for the new tail route (defaults to the same vehicle type)."
          }
        },
        "required": [
          "route",
          "after"
        ],
        "description": "Input for `routing24_edit_split_route`."
      },
      "EditMergeRoutesInput": {
        "type": "object",
        "properties": {
          "target": {
            "type": "integer",
            "minimum": 1,
            "description": "Route number (1-based) that receives the stops."
          },
          "sources": {
            "type": "array",
            "items": {
              "type": "integer",
              "minimum": 1
            },
            "minItems": 1,
            "description": "Route numbers (1-based) whose stops move onto `target`, left empty\nafterwards."
          }
        },
        "required": [
          "target",
          "sources"
        ],
        "description": "Input for `routing24_edit_merge_routes`."
      },
      "EditMarkStopsInput": {
        "type": "object",
        "properties": {
          "stops": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "description": "Planned stop business ids."
          }
        },
        "required": [
          "stops"
        ],
        "description": "Input for `routing24_edit_mark_user_assigned` / `..._clear_user_assigned`."
      },
      "EditResult": {
        "type": "object",
        "properties": {
          "applied": {
            "type": "boolean",
            "description": "True when the whole batch committed; false = nothing was applied."
          },
          "errorCode": {
            "oneOf": [
              {
                "const": "solve_in_progress"
              },
              {
                "const": "no_solution"
              },
              {
                "const": "slot_out_of_range"
              },
              {
                "const": "route_too_small"
              },
              {
                "const": "optimize_running"
              },
              {
                "const": "nothing_to_undo"
              },
              {
                "const": "nothing_to_redo"
              },
              {
                "const": "session_error"
              }
            ]
          },
          "error": {
            "type": "string",
            "description": "Human-readable failure detail accompanying `errorCode`."
          },
          "rejection": {
            "$ref": "#/components/schemas/EditOpStatus",
            "description": "Why the batch rolled back (all-or-nothing), when the engine rejected it."
          },
          "state": {
            "$ref": "#/components/schemas/SessionState",
            "description": "Current state — post-apply, or unchanged when the batch was rejected."
          },
          "userAssignedReports": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/UserAssignedProblemReport"
            },
            "description": "Problem reports for all currently user-assigned sites (when `applied`)."
          }
        },
        "required": [
          "applied"
        ],
        "description": "Shared result of every `routing24_edit_*` tool."
      },
      "EditOpStatus": {
        "type": "object",
        "properties": {
          "edit": {
            "type": "number",
            "description": "Index into the submitted `ops` array."
          },
          "code": {
            "$ref": "#/components/schemas/EditRejectionCode"
          },
          "reason": {
            "type": "string",
            "description": "Human-readable rejection detail, when the engine supplies one."
          }
        },
        "required": [
          "edit",
          "code"
        ],
        "description": "The rejected op of a failed batch (edits.fbs `EditStatus`)."
      },
      "EditRejectionCode": {
        "oneOf": [
          {
            "const": "slot_out_of_range"
          },
          {
            "const": "nothing_to_undo"
          },
          {
            "const": "nothing_to_redo"
          },
          {
            "const": "unknown_site"
          },
          {
            "const": "unknown_vehicle"
          },
          {
            "const": "bad_anchor"
          },
          {
            "const": "anchor_not_assigned"
          },
          {
            "const": "anchor_in_moved_sites"
          },
          {
            "const": "ambiguous_destination"
          },
          {
            "const": "missing_destination"
          },
          {
            "const": "destination_mismatch"
          },
          {
            "const": "route_not_empty"
          },
          {
            "const": "vehicle_overused"
          },
          {
            "const": "group_conflict"
          },
          {
            "const": "stale_revision"
          },
          {
            "const": "too_many_trips"
          },
          {
            "const": "overlapping_slots"
          },
          {
            "const": "empty_split"
          },
          {
            "const": "site_not_assigned"
          },
          {
            "const": "optimize_in_progress"
          },
          {
            "const": "not_sole_op"
          }
        ],
        "description": "Why an edit batch was rejected (edits.fbs `EditRejection`, minus `none`).\nNotable: `stale_revision` = the plan changed under you — re-read\n`routing24_status` and rebuild the batch; `optimize_in_progress` = wait for\nthe running optimization; `slot_out_of_range`/`overlapping_slots` mean your\nroute numbers are outdated."
      },
      "SessionState": {
        "type": "object",
        "properties": {
          "revision": {
            "type": "number",
            "description": "Session revision after the operation."
          },
          "undoDepth": {
            "type": "number"
          },
          "redoDepth": {
            "type": "number"
          },
          "feasible": {
            "type": "boolean"
          },
          "problemsCount": {
            "type": "number",
            "description": "Total constraint-problem markers across all routes (0 = feasible)."
          },
          "distance": {
            "type": "number"
          },
          "distanceUnit": {
            "oneOf": [
              {
                "const": "km"
              },
              {
                "const": "mi"
              }
            ]
          },
          "durationHours": {
            "type": "number"
          },
          "unassignedCount": {
            "type": "number"
          },
          "unassigned": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Ids of the sites currently unassigned."
          },
          "cost": {
            "$ref": "#/components/schemas/SolutionCost",
            "description": "Economic cost (money). Coverage is `unassignedCount`, never a cost."
          },
          "objective": {
            "$ref": "#/components/schemas/SolverObjective",
            "description": "Solver comparison scalar — see {@link SolverObjective}; never money."
          },
          "userAssigned": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "userUnassigned": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "routes": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RouteSummary"
            },
            "description": "Every route, fresh — supersedes route numbers from before the mutation."
          },
          "driftFromOptimized": {
            "$ref": "#/components/schemas/OptimizedDrift",
            "description": "Drift vs the last full optimization — report `degraded`/`severe`."
          }
        },
        "required": [],
        "description": "Post-mutation session snapshot shared by the editing tools' results."
      },
      "RouteSummary": {
        "type": "object",
        "properties": {
          "route": {
            "type": "number",
            "description": "1-based route number (fresh — valid for the next edit call)."
          },
          "vehicleId": {
            "type": "string"
          },
          "stops": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Ordered site ids (depots excluded); empty = empty route."
          },
          "distance": {
            "type": "number",
            "description": "Total travel distance, in `SessionState.distanceUnit`."
          },
          "durationHours": {
            "type": "number",
            "description": "Total duration (travel + service + wait), hours."
          },
          "feasible": {
            "type": "boolean"
          },
          "problems": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PlanProblem"
            }
          },
          "cost": {
            "$ref": "#/components/schemas/RouteCost",
            "description": "Economic cost of this route (absent on pre-feature solutions)."
          }
        },
        "required": [
          "route",
          "stops"
        ],
        "description": "One route's summary in a {@link SessionState} (post-edit snapshot)."
      },
      "UserAssignedProblemReport": {
        "type": "object",
        "properties": {
          "stop": {
            "type": "string"
          },
          "route": {
            "type": "number",
            "description": "Route number (1-based) the stop sits on."
          },
          "intrinsic": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PlanProblem"
            }
          },
          "introduced": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PlanProblem"
            }
          }
        },
        "required": [
          "stop",
          "intrinsic",
          "introduced"
        ],
        "description": "The problem report behind a user-assigned site's badge (edits.fbs\n`UserAssignedReport`): what placing it costs, split into problems the site\nwould have anywhere (`intrinsic`) vs ones this placement introduced."
      },
      "ReoptimizeRouteInput": {
        "type": "object",
        "properties": {
          "route": {
            "type": "integer",
            "minimum": 1,
            "description": "Route number to optimize — 1-based, as in\n`OptimizeStatus.routeStats[].route` and the UI; needs ≥2 stops."
          },
          "time_limit_s": {
            "type": "integer",
            "minimum": 1,
            "maximum": 60,
            "description": "Wall budget in seconds; omit to auto-scale with route size (1.5–5s)."
          }
        },
        "required": [
          "route"
        ],
        "description": "Input for `routing24_reoptimize_route`: re-sequence ONE route's stops."
      },
      "ReoptimizeRouteResult": {
        "type": "object",
        "properties": {
          "started": {
            "type": "boolean",
            "description": "False when the run could not start (see `errorCode`/`error`)."
          },
          "errorCode": {
            "oneOf": [
              {
                "const": "solve_in_progress"
              },
              {
                "const": "no_solution"
              },
              {
                "const": "slot_out_of_range"
              },
              {
                "const": "route_too_small"
              },
              {
                "const": "optimize_running"
              },
              {
                "const": "nothing_to_undo"
              },
              {
                "const": "nothing_to_redo"
              },
              {
                "const": "session_error"
              }
            ]
          },
          "error": {
            "type": "string"
          },
          "state": {
            "$ref": "#/components/schemas/SessionState",
            "description": "Post-run state (present when `started`)."
          }
        },
        "required": [
          "started"
        ],
        "description": "Result of `routing24_reoptimize_route`."
      },
      "HistoryResult": {
        "type": "object",
        "properties": {
          "applied": {
            "type": "boolean",
            "description": "False when nothing was undone/redone (see `errorCode`)."
          },
          "errorCode": {
            "oneOf": [
              {
                "const": "solve_in_progress"
              },
              {
                "const": "no_solution"
              },
              {
                "const": "slot_out_of_range"
              },
              {
                "const": "route_too_small"
              },
              {
                "const": "optimize_running"
              },
              {
                "const": "nothing_to_undo"
              },
              {
                "const": "nothing_to_redo"
              },
              {
                "const": "session_error"
              }
            ]
          },
          "error": {
            "type": "string"
          },
          "state": {
            "$ref": "#/components/schemas/SessionState"
          }
        },
        "required": [
          "applied"
        ],
        "description": "Result of `routing24_undo` / `routing24_redo`."
      },
      "ListPageInput": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Case-insensitive substring over the id and address fields."
          },
          "offset": {
            "type": "integer",
            "minimum": 0,
            "description": "0-based row offset into the filtered list."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "description": "Rows per page (default 30)."
          }
        },
        "required": [],
        "description": "Input for the four `routing24_list_*` tools — one paging/filter shape, and\neach tool returns ONE row type. (A single list tool taking a `kind` would\nswitch its own result shape, so a caller could not know what it would get\nback without re-reading the input it had just sent.)"
      },
      "ListStopsResult": {
        "type": "object",
        "properties": {
          "total": {
            "type": "number",
            "description": "Rows matching the filter, before paging."
          },
          "offset": {
            "type": "number"
          },
          "returned": {
            "type": "number"
          },
          "stops": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StopRow"
            }
          }
        },
        "required": [
          "total",
          "offset",
          "returned",
          "stops"
        ],
        "description": "Result of `routing24_list_stops`. Rows use the SAME shape\n`routing24_upsert_stops` accepts, so read-modify-write needs no field\nmapping — including `area`."
      },
      "StopRow": {
        "type": "object",
        "properties": {
          "address": {
            "type": "string",
            "description": "The WHOLE place the user named, spelling fixed — every part they wrote\n(building, tower, unit, street). Order words riding in the same line\n(\"pickup 2\", a quantity) are NOT part of the address and never become\none: they belong in the load fields."
          },
          "area": {
            "type": "string",
            "description": "Optional area/city qualifier shown after the address (the address book's\n`address`+`area` identity). The `routing24_list_*` tools return it on\nevery stop/depot row, so accepting it here is what makes that row\nwritable back through `routing24_upsert_stops`/`_depots` unchanged."
          },
          "pickup": {
            "type": "number",
            "description": "Load picked UP at this stop and carried back to the depot, in the\nsame unit as vehicle capacity (\"pickup 2\" on an order means\n`pickup: 2` — a load field, never part of the id or address)."
          },
          "delivery": {
            "type": "number",
            "description": "Load taken from the depot and DELIVERED at this stop, in the same\nunit as vehicle capacity."
          },
          "service_duration_s": {
            "type": "number"
          },
          "tw_early_s": {
            "type": "number",
            "description": "Window opens, seconds since midnight. Absent = opens with the day."
          },
          "tw_late_s": {
            "type": "number",
            "description": "Window closes, seconds since midnight. Absent = open-ended (no\nclosing bound on this stop)."
          },
          "release_time_s": {
            "type": "number"
          },
          "priority": {
            "type": "number"
          },
          "required_tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "forbidden_tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "group": {
            "type": "string"
          },
          "transfer_type": {
            "oneOf": [
              {
                "const": "pickup"
              },
              {
                "const": "delivery"
              },
              {
                "const": "depot"
              }
            ]
          },
          "transfer_id": {
            "type": "string"
          },
          "no_break": {
            "type": "boolean"
          },
          "load_class": {
            "type": "string"
          },
          "sequence_group": {
            "type": "string"
          },
          "sequence_rank": {
            "type": "number"
          },
          "max_time_in_vehicle_s": {
            "type": "integer",
            "minimum": 0
          },
          "max_ride_overtime_s": {
            "type": "integer",
            "minimum": 0
          },
          "id": {
            "type": "string",
            "description": "Caller-chosen business id — the name every other tool refers to.\nONE row per order: never a dumping ground for leftover words (a\nload like \"pickup 1\" is the `pickup` field, not an id and not a\nsecond stop)."
          },
          "status": {
            "oneOf": [
              {
                "const": "geocoded"
              },
              {
                "const": "ungeocoded"
              }
            ],
            "description": "Whether the row's address resolved to a map location. Reported on\n`routing24_list_stops` rows; accepted and ignored on upsert (so a\nlisted row writes back unchanged)."
          }
        },
        "required": [],
        "description": "A delivery/pickup stop: a place plus solver site constraints.\n\nThe intersection above is flattened in the generated schema, which drops the\ndoc comments of the types it merges — and the schema is all a tool caller\nsees, so the conventions those types state are repeated here:\nthe location is the `address` string, geocoded internally; `id` is the\nbusiness id the caller chooses and every other tool refers to (there is no\nname field); loads go in `delivery` (loaded at the depot, dropped here) and\n`pickup` (taken on here, carried back to the depot); every `*_s` time field\nis in seconds — `tw_early_s`/`tw_late_s`/`release_time_s` counted from\nmidnight, `service_duration_s` a duration."
      },
      "ListVehiclesResult": {
        "type": "object",
        "properties": {
          "total": {
            "type": "number",
            "description": "Rows matching the filter, before paging."
          },
          "offset": {
            "type": "number"
          },
          "returned": {
            "type": "number"
          },
          "vehicles": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/VehicleRow"
            }
          }
        },
        "required": [
          "total",
          "offset",
          "returned",
          "vehicles"
        ],
        "description": "Result of `routing24_list_vehicles` (rows accepted by `routing24_upsert_vehicles`)."
      },
      "VehicleRow": {
        "type": "object",
        "properties": {
          "tw_early_s": {
            "type": "number",
            "description": "Shift starts, seconds since midnight. Absent = no earliest start."
          },
          "tw_late_s": {
            "type": "number",
            "description": "Shift ends, seconds since midnight. Absent = open-ended: no\nshift-end limit at all, the strongest relaxation of the shift."
          },
          "capacity": {
            "type": "number"
          },
          "available_count": {
            "type": "number"
          },
          "cost": {
            "$ref": "#/components/schemas/solver.VehicleCost"
          },
          "start_late_s": {
            "type": "number",
            "description": "Latest time the vehicle may start its shift (leave the depot),\ndistinct from tw_late_s (latest shift end). Absent = defaults to\ntw_late_s solver-side."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "max_reloads": {
            "type": "number"
          },
          "max_duration_s": {
            "type": "number",
            "description": "Max route duration, seconds. Absent = uncapped."
          },
          "max_overtime_s": {
            "type": "number"
          },
          "max_distance": {
            "type": "number",
            "description": "Max route distance in the plan's display unit. Absent = uncapped."
          },
          "break_rules": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/VehicleBreakRule"
            }
          },
          "fixed_breaks": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/VehicleFixedBreak"
            }
          },
          "period_driving_limit_s": {
            "type": "number"
          },
          "period_driven_s": {
            "type": "number"
          },
          "no_mix_load_classes": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LoadClassGroup"
            }
          },
          "id": {
            "type": "string"
          },
          "force_allow_sites": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Stop ids this vehicle type may serve even when tags forbid it — a\nPERMISSION override, not a reservation: other compatible vehicles can\nstill take the stop. Precedence per vehicle: force_deny_sites beats\nforce_allow_sites beats the tag rule. To make a stop exclusive to one\nvehicle, use tags (required_tags on the stop + that tag on only this\nvehicle) or force_deny_sites on every other vehicle."
          },
          "force_deny_sites": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Stop ids this vehicle type must never serve (beats force_allow_sites and tags)."
          },
          "reload_depots": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Depot ids where mid-route reloads may happen (multi-trip). Omitted =\nsingle-trip unless `max_reloads` is set (then the home depot is used);\npair with `max_reloads` to cap trips."
          },
          "start_depot_id": {
            "type": "string",
            "description": "Business id of the start/finish depot (see `routing24_upsert_vehicles`)."
          },
          "end_depot_id": {
            "type": "string"
          }
        },
        "required": [],
        "description": "A vehicle row: the upsert shape plus the resolved depot business ids."
      },
      "solver.VehicleCost": {
        "type": "object",
        "properties": {
          "fixed": {
            "type": "number",
            "minimum": 0,
            "description": "Cost per use of the vehicle; charged only when it drives a route."
          },
          "distance": {
            "type": "number",
            "minimum": 0,
            "description": "Cost per km (or per mile when the plan's display unit is miles)."
          },
          "duration": {
            "type": "number",
            "minimum": 0,
            "description": "Cost per hour of route time."
          },
          "stop": {
            "type": "number",
            "minimum": 0,
            "description": "Cost per stop served (each order visit; a transfer pair counts its\npickup and delivery separately). Depot and reload visits are free.\nNo unit conversion: charged per visit in your cost units."
          },
          "overtime": {
            "type": "number",
            "minimum": 0,
            "description": "Surcharge per hour of overtime, paired with the vehicle's\n`max_overtime_s` cap. When overtime is allowed and this is unset, it\ndefaults to the `duration` rate (or a nominal 1/hour if that is 0)\nso allowed overtime is never free; an explicit 0 makes it free."
          },
          "ride_overtime": {
            "type": "number",
            "minimum": 0,
            "description": "Cost per hour of a linked (pickup & delivery) order's ride time\ninside its overtime band (`max_ride_overtime_s`, past\n`max_time_in_vehicle_s`). Unset/0 => that band is free."
          },
          "load_distance": {
            "type": "number",
            "minimum": 0,
            "description": "Cost per unit of load carried per km (or per mile when the plan's\ndisplay unit is miles): every leg charges the load on board times\nthe leg's length, so heavier cargo is priced onto shorter paths\nand dropped early. Models load-dependent fuel/energy burn and\nrefrigerated (cold chain) running cost. Counts toward the fleet's\n\"priced\" signal like `distance`/`duration`. Unset/0 => carriage\nis free."
          }
        },
        "required": [],
        "description": "Per-vehicle cost rates. An omitted field means 0. Exception: when NO\nvehicle in the fleet has a nonzero `distance`, `duration` or\n`load_distance` rate, the optimizer falls back to the default rates of\n1 per km (or mile) plus 1 per hour, minimizing distance and time\ntogether (so an all-blank fleet still gets sensible routes); setting\nevery cost to an explicit 0 triggers the same fallback plus a warning.\nAn explicit 0 is meaningful in a mixed fleet (e.g. a bike with free\nmileage next to a priced van)."
      },
      "VehicleBreakRule": {
        "type": "object",
        "properties": {
          "max_driving_s": {
            "type": "number"
          },
          "duration_s": {
            "type": "number"
          },
          "split_first_s": {
            "type": "number"
          },
          "split_second_s": {
            "type": "number"
          },
          "service_counts": {
            "type": "boolean"
          }
        },
        "required": []
      },
      "VehicleFixedBreak": {
        "type": "object",
        "properties": {
          "tw_early_s": {
            "type": "number"
          },
          "tw_late_s": {
            "type": "number"
          },
          "duration_s": {
            "type": "number"
          }
        },
        "required": []
      },
      "LoadClassGroup": {
        "type": "object",
        "properties": {
          "classes": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "classes"
        ]
      },
      "ListDepotsResult": {
        "type": "object",
        "properties": {
          "total": {
            "type": "number",
            "description": "Rows matching the filter, before paging."
          },
          "offset": {
            "type": "number"
          },
          "returned": {
            "type": "number"
          },
          "depots": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DepotRow"
            }
          }
        },
        "required": [
          "total",
          "offset",
          "returned",
          "depots"
        ],
        "description": "Result of `routing24_list_depots` (rows accepted by `routing24_upsert_depots`)."
      },
      "DepotRow": {
        "type": "object",
        "properties": {
          "address": {
            "type": "string",
            "description": "The WHOLE place the user named, spelling fixed — every part they wrote\n(building, tower, unit, street). Order words riding in the same line\n(\"pickup 2\", a quantity) are NOT part of the address and never become\none: they belong in the load fields."
          },
          "area": {
            "type": "string",
            "description": "Optional area/city qualifier shown after the address (the address book's\n`address`+`area` identity). The `routing24_list_*` tools return it on\nevery stop/depot row, so accepting it here is what makes that row\nwritable back through `routing24_upsert_stops`/`_depots` unchanged."
          },
          "service_duration_s": {
            "type": "number",
            "description": "Handling time at the depot, seconds. Absent = none."
          },
          "tw_early_s": {
            "type": "number",
            "description": "Opens at, seconds since midnight. Absent = open from any time."
          },
          "tw_late_s": {
            "type": "number",
            "description": "Closes at, seconds since midnight. Absent = open-ended: the depot\nnever closes and the return-by-close constraint is fully lifted."
          },
          "no_break": {
            "type": "boolean"
          },
          "id": {
            "type": "string"
          },
          "status": {
            "oneOf": [
              {
                "const": "geocoded"
              },
              {
                "const": "ungeocoded"
              }
            ]
          }
        },
        "required": [],
        "description": "A depot row: a place plus depot constraints (open window, handling).\nSame conventions as {@link StopRow}: an `address` geocoded internally, a\ncaller-chosen `id`, `tw_early_s`/`tw_late_s` in seconds since midnight and\n`service_duration_s` in seconds. `status` is reported on\n`routing24_list_depots` rows; accepted and ignored on upsert."
      },
      "ListAddressesResult": {
        "type": "object",
        "properties": {
          "total": {
            "type": "number",
            "description": "Rows matching the filter, before paging."
          },
          "offset": {
            "type": "number"
          },
          "returned": {
            "type": "number"
          },
          "addresses": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AddressRow"
            }
          }
        },
        "required": [
          "total",
          "offset",
          "returned",
          "addresses"
        ],
        "description": "Result of `routing24_list_addresses` (rows accepted by `routing24_upsert_addresses`)."
      },
      "AddressRow": {
        "type": "object",
        "properties": {
          "address": {
            "type": "string"
          },
          "area": {
            "type": "string",
            "description": "Optional area/city qualifier displayed after the address."
          },
          "status": {
            "oneOf": [
              {
                "const": "geocoded"
              },
              {
                "const": "ungeocoded"
              }
            ],
            "description": "Whether the row resolved to a map location. Reported on\n`routing24_list_addresses` rows; accepted and ignored on upsert."
          },
          "usedBySites": {
            "type": "number"
          },
          "usedByDepots": {
            "type": "number"
          }
        },
        "required": [
          "address"
        ],
        "description": "An address row: the upsert shape plus how many entities reference it."
      },
      "UpsertStopsInput": {
        "type": "object",
        "properties": {
          "stops": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StopPatch"
            },
            "minItems": 1
          }
        },
        "required": [
          "stops"
        ],
        "description": "Input for `routing24_upsert_stops`."
      },
      "StopPatch": {
        "type": "object",
        "properties": {
          "pickup": {
            "type": "number",
            "description": "Load picked UP at this stop and carried back to the depot, in the\nsame unit as vehicle capacity (\"pickup 2\" on an order means\n`pickup: 2` — a load field, never part of the id or address)."
          },
          "delivery": {
            "type": "number",
            "description": "Load taken from the depot and DELIVERED at this stop, in the same\nunit as vehicle capacity."
          },
          "service_duration_s": {
            "type": "number"
          },
          "priority": {
            "type": "number"
          },
          "required_tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "forbidden_tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "group": {
            "type": "string"
          },
          "transfer_type": {
            "oneOf": [
              {
                "const": "pickup"
              },
              {
                "const": "delivery"
              },
              {
                "const": "depot"
              }
            ]
          },
          "transfer_id": {
            "type": "string"
          },
          "no_break": {
            "type": "boolean"
          },
          "load_class": {
            "type": "string"
          },
          "sequence_group": {
            "type": "string"
          },
          "sequence_rank": {
            "type": "number"
          },
          "address": {
            "type": "string",
            "description": "The WHOLE place the user named, spelling fixed — every part they wrote\n(building, tower, unit, street). Order words riding in the same line\n(\"pickup 2\", a quantity) are NOT part of the address and never become\none: they belong in the load fields."
          },
          "area": {
            "type": "string",
            "description": "Optional area/city qualifier shown after the address (the address book's\n`address`+`area` identity). The `routing24_list_*` tools return it on\nevery stop/depot row, so accepting it here is what makes that row\nwritable back through `routing24_upsert_stops`/`_depots` unchanged."
          },
          "max_time_in_vehicle_s": {
            "type": "integer",
            "minimum": 0
          },
          "max_ride_overtime_s": {
            "type": "integer",
            "minimum": 0
          },
          "id": {
            "type": "string",
            "description": "Caller-chosen business id — the name every other tool refers to.\nONE row per order: never a dumping ground for leftover words (a\nload like \"pickup 1\" is the `pickup` field, not an id and not a\nsecond stop)."
          },
          "status": {
            "oneOf": [
              {
                "const": "geocoded"
              },
              {
                "const": "ungeocoded"
              }
            ],
            "description": "Whether the row's address resolved to a map location. Reported on\n`routing24_list_stops` rows; accepted and ignored on upsert (so a\nlisted row writes back unchanged)."
          },
          "tw_early_s": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "type": "number"
              }
            ]
          },
          "tw_late_s": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "type": "number"
              }
            ]
          },
          "release_time_s": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "type": "number"
              }
            ]
          }
        },
        "required": [],
        "description": "{@link StopRow} with clearable bounds (see above)."
      },
      "UpsertDepotsInput": {
        "type": "object",
        "properties": {
          "depots": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DepotPatch"
            },
            "minItems": 1
          }
        },
        "required": [
          "depots"
        ],
        "description": "Input for `routing24_upsert_depots`."
      },
      "DepotPatch": {
        "type": "object",
        "properties": {
          "service_duration_s": {
            "type": "number",
            "description": "Handling time at the depot, seconds. Absent = none."
          },
          "no_break": {
            "type": "boolean"
          },
          "address": {
            "type": "string",
            "description": "The WHOLE place the user named, spelling fixed — every part they wrote\n(building, tower, unit, street). Order words riding in the same line\n(\"pickup 2\", a quantity) are NOT part of the address and never become\none: they belong in the load fields."
          },
          "area": {
            "type": "string",
            "description": "Optional area/city qualifier shown after the address (the address book's\n`address`+`area` identity). The `routing24_list_*` tools return it on\nevery stop/depot row, so accepting it here is what makes that row\nwritable back through `routing24_upsert_stops`/`_depots` unchanged."
          },
          "id": {
            "type": "string"
          },
          "status": {
            "oneOf": [
              {
                "const": "geocoded"
              },
              {
                "const": "ungeocoded"
              }
            ]
          },
          "tw_early_s": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "type": "number"
              }
            ]
          },
          "tw_late_s": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "type": "number"
              }
            ]
          }
        },
        "required": [],
        "description": "{@link DepotRow} with clearable bounds (see above)."
      },
      "UpsertVehiclesInput": {
        "type": "object",
        "properties": {
          "vehicles": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/VehiclePatch"
            },
            "minItems": 1
          }
        },
        "required": [
          "vehicles"
        ],
        "description": "Input for `routing24_upsert_vehicles`. `start_depot_id`/`end_depot_id` take\ndepot business ids and may be omitted when the plan has exactly one depot."
      },
      "VehiclePatch": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "capacity": {
            "type": "number"
          },
          "available_count": {
            "type": "number"
          },
          "cost": {
            "$ref": "#/components/schemas/solver.VehicleCost"
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "max_reloads": {
            "type": "number"
          },
          "max_overtime_s": {
            "type": "number"
          },
          "break_rules": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/VehicleBreakRule"
            }
          },
          "fixed_breaks": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/VehicleFixedBreak"
            }
          },
          "period_driving_limit_s": {
            "type": "number"
          },
          "period_driven_s": {
            "type": "number"
          },
          "no_mix_load_classes": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LoadClassGroup"
            }
          },
          "force_allow_sites": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Stop ids this vehicle type may serve even when tags forbid it — a\nPERMISSION override, not a reservation: other compatible vehicles can\nstill take the stop. Precedence per vehicle: force_deny_sites beats\nforce_allow_sites beats the tag rule. To make a stop exclusive to one\nvehicle, use tags (required_tags on the stop + that tag on only this\nvehicle) or force_deny_sites on every other vehicle."
          },
          "force_deny_sites": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Stop ids this vehicle type must never serve (beats force_allow_sites and tags)."
          },
          "reload_depots": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Depot ids where mid-route reloads may happen (multi-trip). Omitted =\nsingle-trip unless `max_reloads` is set (then the home depot is used);\npair with `max_reloads` to cap trips."
          },
          "start_depot_id": {
            "type": "string",
            "description": "Business id of the start/finish depot (see `routing24_upsert_vehicles`)."
          },
          "end_depot_id": {
            "type": "string"
          },
          "tw_early_s": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "type": "number"
              }
            ]
          },
          "tw_late_s": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "type": "number"
              }
            ]
          },
          "start_late_s": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "type": "number"
              }
            ]
          },
          "max_duration_s": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "type": "number"
              }
            ]
          },
          "max_distance": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "type": "number"
              }
            ]
          }
        },
        "required": [],
        "description": "{@link VehicleRow} with clearable bounds (see above)."
      },
      "UpsertAddressesInput": {
        "type": "object",
        "properties": {
          "addresses": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AddressUpsert"
            },
            "minItems": 1
          }
        },
        "required": [
          "addresses"
        ],
        "description": "Input for `routing24_upsert_addresses` (identity is the `address`+`area` pair)."
      },
      "AddressUpsert": {
        "type": "object",
        "properties": {
          "address": {
            "type": "string"
          },
          "area": {
            "type": "string",
            "description": "Optional area/city qualifier displayed after the address."
          },
          "status": {
            "oneOf": [
              {
                "const": "geocoded"
              },
              {
                "const": "ungeocoded"
              }
            ],
            "description": "Whether the row resolved to a map location. Reported on\n`routing24_list_addresses` rows; accepted and ignored on upsert."
          }
        },
        "required": [
          "address"
        ],
        "description": "One address-book row for `routing24_upsert_addresses` (and the row shape\n`routing24_list_addresses` returns). The row's identity\nis the `address` (+ `area`) string pair — upserting the same pair updates\nthe existing row."
      },
      "UpsertResult": {
        "type": "object",
        "properties": {
          "applied": {
            "type": "boolean"
          },
          "error": {
            "type": "string"
          },
          "added": {
            "type": "number"
          },
          "updated": {
            "type": "number"
          },
          "skipped": {
            "type": "number",
            "description": "Rows dropped (e.g. malformed beyond repair)."
          },
          "unresolvedRefs": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Unresolved cross-references, as human-readable messages."
          },
          "geocoded": {
            "type": "number",
            "description": "How many entities were geocoded from their address on the way in\n(rows whose address could not be geocoded are still saved — see\n`addressDiagnostics`)."
          },
          "addressDiagnostics": {
            "$ref": "#/components/schemas/AddressDiagnostics",
            "description": "Present only when the batch left address problems behind."
          },
          "fleetDiagnostics": {
            "$ref": "#/components/schemas/FleetDiagnostics",
            "description": "Present only when the edit left stops no vehicle can serve."
          }
        },
        "required": [
          "applied",
          "added",
          "updated",
          "skipped"
        ],
        "description": "Result of every `routing24_upsert_*` tool."
      },
      "AddressDiagnostics": {
        "type": "object",
        "properties": {
          "summary": {
            "type": "string",
            "description": "One host-composed sentence with the counts."
          },
          "problems": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "category": {
                  "oneOf": [
                    {
                      "const": "ungeocoded"
                    },
                    {
                      "const": "far_from_plan"
                    }
                  ],
                  "description": "`ungeocoded` = rows saved without a location (optimizing is blocked\nwhile a stop or depot uses one). `far_from_plan` = geocoded far from\nthe rest of the plan — usually a geocoding miss, occasionally\nintended."
                },
                "count": {
                  "type": "number"
                },
                "explanation": {
                  "type": "string",
                  "description": "What this means for the plan, one sentence."
                },
                "lever": {
                  "type": "string",
                  "description": "The exact call that addresses it (offer it to the user first)."
                }
              },
              "required": [
                "category",
                "count",
                "explanation",
                "lever"
              ]
            }
          }
        },
        "required": [
          "summary",
          "problems"
        ],
        "description": "Address problems the upsert path detected, host-computed (the geocoder and\nthe geo analysis are black boxes — this block is their only agent-visible\noutput). Carries counts and next calls, never the rows themselves: the user\nsees the affected addresses in the app and in the `fix_addresses` review\ndialog, so never enumerate them in chat or in an ask_user question."
      },
      "FleetDiagnostics": {
        "type": "object",
        "properties": {
          "summary": {
            "type": "string",
            "description": "One host-composed sentence with the counts."
          },
          "problems": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "category": {
                  "const": "vehicle_incompatible",
                  "description": "`vehicle_incompatible` = stops no vehicle in the fleet can serve\n(same name the solver reports post-solve for such stops)."
                },
                "count": {
                  "type": "number"
                },
                "explanation": {
                  "type": "string",
                  "description": "What this means for the plan, one sentence."
                },
                "lever": {
                  "type": "string",
                  "description": "The exact call that addresses it (offer it to the user first)."
                }
              },
              "required": [
                "category",
                "count",
                "explanation",
                "lever"
              ]
            }
          }
        },
        "required": [
          "summary",
          "problems"
        ],
        "description": "Tag/fleet compatibility problems a data edit left behind, host-computed\nfrom the same predicate the solver applies (required_tags/forbidden_tags\nplus the force-allow/deny overrides). Carries counts and next calls, never\nthe rows: the user sees the affected stops in the app. Absent while the\nplan has no vehicles at all — an empty fleet is a normal intermediate\nstate, not a compatibility problem."
      },
      "UpsertAddressesResult": {
        "type": "object",
        "properties": {
          "applied": {
            "type": "boolean"
          },
          "error": {
            "type": "string"
          },
          "added": {
            "type": "number"
          },
          "updated": {
            "type": "number"
          },
          "skipped": {
            "type": "number",
            "description": "Rows dropped (e.g. malformed beyond repair)."
          },
          "unresolvedRefs": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Unresolved cross-references, as human-readable messages."
          },
          "geocoded": {
            "type": "number",
            "description": "How many entities were geocoded from their address on the way in\n(rows whose address could not be geocoded are still saved — see\n`addressDiagnostics`)."
          },
          "addressDiagnostics": {
            "$ref": "#/components/schemas/AddressDiagnostics",
            "description": "Present only when the batch left address problems behind."
          },
          "fleetDiagnostics": {
            "$ref": "#/components/schemas/FleetDiagnostics",
            "description": "Present only when the edit left stops no vehicle can serve."
          },
          "rows": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "address": {
                  "type": "string",
                  "description": "The `address` as sent (the row's identity, with `area`)."
                },
                "area": {
                  "type": "string"
                },
                "status": {
                  "oneOf": [
                    {
                      "const": "geocoded"
                    },
                    {
                      "const": "ungeocoded"
                    }
                  ]
                },
                "matched": {
                  "type": "string",
                  "description": "Canonical address the geocoder matched, when this call geocoded the row."
                }
              },
              "required": [
                "address",
                "status"
              ]
            }
          }
        },
        "required": [
          "applied",
          "added",
          "updated",
          "skipped"
        ],
        "description": "Result of `routing24_upsert_addresses`: the shared upsert counts plus, for\nSMALL batches only (at most 5 rows — the confirm-with-the-user use), one\nresult per input row. `matched` is the canonical address the geocoder\nresolved for rows geocoded on this call — state it when confirming a\ndoubtful address with the user; it is transient and never stored. Rows that\nreused an already-located address book entry come back `status: \"geocoded\"`\nwith no `matched`. Larger batches return counts and `addressDiagnostics`\nonly."
      },
      "DeleteByIdsInput": {
        "type": "object",
        "properties": {
          "ids": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "minItems": 1
          }
        },
        "required": [
          "ids"
        ],
        "description": "Input for the four `routing24_delete_*` tools: business ids as returned by\nthe matching `routing24_list_*` (for addresses, the `address` — or\n`address, area` — string)."
      },
      "DeleteResult": {
        "type": "object",
        "properties": {
          "deleted": {
            "type": "number"
          },
          "notFound": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Ids that matched nothing (nothing was deleted for them)."
          },
          "cascade": {
            "type": "string",
            "description": "What was removed alongside (deletes cascade: a depot delete removes\nvehicles referencing it; an address delete removes the depots/stops at\nit; a stop delete drops its address when nothing else uses it)."
          },
          "fleetDiagnostics": {
            "$ref": "#/components/schemas/FleetDiagnostics",
            "description": "Present only when the delete left stops no vehicle can serve."
          }
        },
        "required": [
          "deleted"
        ],
        "description": "Result of every `routing24_delete_*` tool."
      },
      "ReoptimizePlanInput": {
        "type": "object",
        "properties": {
          "options": {
            "type": "object",
            "properties": {
              "time_limit_s": {
                "type": "integer",
                "minimum": 1,
                "description": "Solve budget in seconds; omit to auto-scale with problem size."
              }
            },
            "required": []
          }
        },
        "required": [],
        "description": "Input for `routing24_reoptimize_plan`."
      },
      "OpenPageInput": {
        "type": "object",
        "properties": {
          "page": {
            "oneOf": [
              {
                "const": "home"
              },
              {
                "const": "plans"
              },
              {
                "const": "addresses"
              },
              {
                "const": "stops"
              },
              {
                "const": "depots"
              },
              {
                "const": "vehicles"
              },
              {
                "const": "optimize"
              }
            ],
            "description": "The app page to open (\"stops\" is the Orders page)."
          }
        },
        "required": [
          "page"
        ],
        "description": "Input for `routing24_open_page`."
      },
      "ShowOnMapInput": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Business id of a stop, depot, or address-book row."
          }
        },
        "required": [
          "id"
        ],
        "description": "Input for `routing24_show_on_map`."
      },
      "ShowOnMapResult": {
        "type": "object",
        "properties": {
          "shown": {
            "type": "boolean"
          },
          "kind": {
            "oneOf": [
              {
                "const": "address"
              },
              {
                "const": "depot"
              },
              {
                "const": "stop"
              }
            ],
            "description": "What the id resolved to."
          },
          "error": {
            "type": "string"
          }
        },
        "required": [
          "shown"
        ],
        "description": "Result of `routing24_show_on_map`."
      },
      "ExportExcelResult": {
        "type": "object",
        "properties": {
          "exported": {
            "type": "boolean"
          },
          "fileName": {
            "type": "string",
            "description": "The browser download's file name."
          },
          "kind": {
            "oneOf": [
              {
                "const": "solution"
              },
              {
                "const": "plan"
              }
            ],
            "description": "`solution` = optimized routes workbook; `plan` = plan data workbook."
          },
          "error": {
            "type": "string"
          }
        },
        "required": [
          "exported"
        ],
        "description": "Result of `routing24_export_excel`."
      },
      "LoadPlanInput": {
        "type": "object",
        "properties": {
          "plan_id": {
            "type": "string",
            "description": "The plan's id, as returned by `routing24_list_plans`."
          }
        },
        "required": [
          "plan_id"
        ],
        "description": "Input for `routing24_load_plan`."
      },
      "LoadPlanResult": {
        "type": "object",
        "properties": {
          "loaded": {
            "type": "boolean"
          },
          "plan_id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "alreadyLoaded": {
            "type": "boolean",
            "description": "The plan was already the loaded plan; nothing changed."
          },
          "error": {
            "type": "string"
          }
        },
        "required": [
          "loaded",
          "plan_id"
        ],
        "description": "Result of `routing24_load_plan`."
      },
      "PlanListEntry": {
        "type": "object",
        "properties": {
          "plan_id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "createdAt": {
            "type": "number",
            "description": "Unix epoch milliseconds."
          },
          "updatedAt": {
            "type": "number",
            "description": "Unix epoch milliseconds."
          },
          "entitiesCount": {
            "type": "object",
            "properties": {
              "addresses": {
                "type": "number"
              },
              "vehicles": {
                "type": "number"
              },
              "depots": {
                "type": "number"
              },
              "sites": {
                "type": "number"
              }
            },
            "required": [
              "addresses",
              "vehicles",
              "depots",
              "sites"
            ]
          },
          "current": {
            "type": "boolean",
            "description": "Set on the plan currently loaded in the app."
          }
        },
        "required": [
          "plan_id",
          "name",
          "createdAt",
          "updatedAt"
        ],
        "description": "One saved plan in the `routing24_list_plans` result."
      },
      "ListPlansResult": {
        "type": "object",
        "properties": {
          "plans": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PlanListEntry"
            }
          },
          "truncated": {
            "type": "boolean",
            "description": "More plans exist beyond this first page of the index."
          }
        },
        "required": [
          "plans"
        ],
        "description": "Result of `routing24_list_plans`."
      },
      "SqlQueryInput": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "One or more SQLite statements. Statements that write plan data are\nrejected — use `routing24_sql_update`."
          }
        },
        "required": [
          "query"
        ],
        "description": "Input for `routing24_sql_query`."
      },
      "SqlUpdateInput": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "One or more SQLite statements; UPDATEs on the writable columns of\nsites/vehicles/depots journal cell changes that apply atomically."
          }
        },
        "required": [
          "query"
        ],
        "description": "Input for `routing24_sql_update`."
      },
      "RunScriptInput": {
        "type": "object",
        "properties": {
          "code": {
            "type": "string",
            "description": "JavaScript/TypeScript for the plan sandbox. Mutate `data` entity\nfields; delete via deleteSites/deleteVehicles/deleteDepots(predicate);\nassign a short JSON-serializable summary to `__output`."
          }
        },
        "required": [
          "code"
        ],
        "description": "Input for `routing24_run_script`."
      },
      "SqlQueryResult": {
        "type": "object",
        "properties": {
          "columns": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "rows": {
            "type": "array",
            "items": {
              "type": "array",
              "items": {
                "$ref": "#/components/schemas/SqlCell"
              }
            }
          },
          "rowCount": {
            "type": "number",
            "description": "Rows the statement produced (before the wire cap)."
          },
          "matchedRows": {
            "type": "number"
          },
          "noop": {
            "type": "string",
            "description": "Set when an UPDATE matched rows but changed nothing."
          },
          "truncated": {
            "type": "string"
          }
        },
        "required": [
          "columns",
          "rows",
          "rowCount"
        ],
        "description": "Result of `routing24_sql_query`."
      },
      "SqlCell": {
        "oneOf": [
          {
            "type": "null"
          },
          {
            "type": "string"
          },
          {
            "type": "number"
          }
        ],
        "description": "One SQL result cell (bigints arrive as numbers, or strings when unsafe)."
      },
      "SqlUpdateResult": {
        "type": "object",
        "properties": {
          "applied": {
            "type": "boolean"
          },
          "changedCells": {
            "type": "number"
          },
          "updatedEntities": {
            "type": "number"
          },
          "matchedRows": {
            "type": "number"
          },
          "noop": {
            "type": "string",
            "description": "Set when the statement matched rows but changed nothing."
          },
          "errors": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CommitErrorRow"
            },
            "description": "Validation rejections — nothing was applied."
          },
          "warnings": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "fleetDiagnostics": {
            "$ref": "#/components/schemas/FleetDiagnostics"
          }
        },
        "required": [
          "applied"
        ],
        "description": "Result of `routing24_sql_update`."
      },
      "CommitErrorRow": {
        "type": "object",
        "properties": {
          "kind": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "message": {
            "type": "string"
          }
        },
        "required": [
          "kind",
          "id",
          "message"
        ],
        "description": "One entity the commit pipeline rejected."
      },
      "RunScriptResult": {
        "type": "object",
        "properties": {
          "applied": {
            "type": "boolean"
          },
          "output": {
            "type": "string",
            "description": "The script's `__output`, JSON-serialized (capped)."
          },
          "stdout": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "journaledWrites": {
            "type": "number"
          },
          "deletionsMarked": {
            "type": "number"
          },
          "updatedEntities": {
            "type": "number"
          },
          "deletedEntities": {
            "type": "number"
          },
          "cascades": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "errors": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CommitErrorRow"
            },
            "description": "Validation rejections — nothing was applied."
          },
          "warnings": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "fleetDiagnostics": {
            "$ref": "#/components/schemas/FleetDiagnostics"
          }
        },
        "required": [
          "applied",
          "journaledWrites"
        ],
        "description": "Result of `routing24_run_script`."
      }
    }
  },
  "schemas": [
    {
      "$ref": "#/components/schemas/NewPlanInput"
    },
    {
      "$ref": "#/components/schemas/NewPlanResult"
    },
    {
      "$ref": "#/components/schemas/SolveStarted"
    },
    {
      "$ref": "#/components/schemas/PaidFeatureReport"
    },
    {
      "$ref": "#/components/schemas/OptimizeStatus"
    },
    {
      "$ref": "#/components/schemas/RouteInput"
    },
    {
      "$ref": "#/components/schemas/PlanSolutionRoute"
    },
    {
      "$ref": "#/components/schemas/UnassignedInput"
    },
    {
      "$ref": "#/components/schemas/UnassignedReport"
    },
    {
      "$ref": "#/components/schemas/EditMoveStopsInput"
    },
    {
      "$ref": "#/components/schemas/EditUnassignStopsInput"
    },
    {
      "$ref": "#/components/schemas/EditSetRouteVehicleInput"
    },
    {
      "$ref": "#/components/schemas/EditCreateRouteInput"
    },
    {
      "$ref": "#/components/schemas/EditRemoveRoutesInput"
    },
    {
      "$ref": "#/components/schemas/EditSplitRouteInput"
    },
    {
      "$ref": "#/components/schemas/EditMergeRoutesInput"
    },
    {
      "$ref": "#/components/schemas/EditMarkStopsInput"
    },
    {
      "$ref": "#/components/schemas/EditResult"
    },
    {
      "$ref": "#/components/schemas/ReoptimizeRouteInput"
    },
    {
      "$ref": "#/components/schemas/ReoptimizeRouteResult"
    },
    {
      "$ref": "#/components/schemas/HistoryResult"
    },
    {
      "$ref": "#/components/schemas/ListPageInput"
    },
    {
      "$ref": "#/components/schemas/ListStopsResult"
    },
    {
      "$ref": "#/components/schemas/ListVehiclesResult"
    },
    {
      "$ref": "#/components/schemas/ListDepotsResult"
    },
    {
      "$ref": "#/components/schemas/ListAddressesResult"
    },
    {
      "$ref": "#/components/schemas/UpsertStopsInput"
    },
    {
      "$ref": "#/components/schemas/UpsertDepotsInput"
    },
    {
      "$ref": "#/components/schemas/UpsertVehiclesInput"
    },
    {
      "$ref": "#/components/schemas/UpsertAddressesInput"
    },
    {
      "$ref": "#/components/schemas/UpsertResult"
    },
    {
      "$ref": "#/components/schemas/UpsertAddressesResult"
    },
    {
      "$ref": "#/components/schemas/DeleteByIdsInput"
    },
    {
      "$ref": "#/components/schemas/DeleteResult"
    },
    {
      "$ref": "#/components/schemas/ReoptimizePlanInput"
    },
    {
      "$ref": "#/components/schemas/OpenPageInput"
    },
    {
      "$ref": "#/components/schemas/ShowOnMapInput"
    },
    {
      "$ref": "#/components/schemas/ShowOnMapResult"
    },
    {
      "$ref": "#/components/schemas/ExportExcelResult"
    },
    {
      "$ref": "#/components/schemas/LoadPlanInput"
    },
    {
      "$ref": "#/components/schemas/LoadPlanResult"
    },
    {
      "$ref": "#/components/schemas/PlanListEntry"
    },
    {
      "$ref": "#/components/schemas/ListPlansResult"
    },
    {
      "$ref": "#/components/schemas/SqlQueryInput"
    },
    {
      "$ref": "#/components/schemas/SqlUpdateInput"
    },
    {
      "$ref": "#/components/schemas/RunScriptInput"
    },
    {
      "$ref": "#/components/schemas/SqlQueryResult"
    },
    {
      "$ref": "#/components/schemas/SqlUpdateResult"
    },
    {
      "$ref": "#/components/schemas/RunScriptResult"
    }
  ]
}
